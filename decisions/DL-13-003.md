# DL-13-003: Patch sürüm v0.1.1 — REQ-001 (deploy volume fix)

- Tarih: 2026-07-31
- Faz: 13 — Release (delta, ↺ REQ-001)
- Mod: AUTOPILOT
- Karar: v0.1.0 → **v0.1.1** (patch bump — SemVer: davranış/FR değişmedi, yalnız bir hata düzeltildi). `package.json.version` ve `docs/13-release-notes.md` güncellendi; "Bilinen sınırlar" bölümünde `/data` volume maddesi "ÇÖZÜLDÜ v0.1.1" olarak işaretlendi.
- Değerlendirilen alternatifler: minor bump (v0.2.0) — reddedildi, kullanıcıya görünür yeni/değişen FR yok, saf bug fix; SemVer patch tanımıyla birebir örtüşüyor.
- Gerekçe: Kural — "patch talebi patch bump" (pipeline-request spec §4). Değişiklik yalnız `deploy/remote-deploy.sh`'de (Faz 12), `src/` dokunulmadı.
- Riskler: Yok.
- Geri alınabilirlik: Yüksek (tek satır version alanı + release notes eki).
- İnsan onayı: Otomatik
- Varsayım mı?: Hayır — sınıflandırma (patch) `docs/00-idea-v2.md`'de zaten gerekçelendirildi.

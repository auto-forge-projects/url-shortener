# DL-16-001: Retro kapsamı — commit-queue yarış koşulu tek AF girdisi olarak işlendi

- Tarih: 2026-07-27
- Faz: 16 — Retrospektif
- Mod: AUTOPILOT
- Karar: Bu koşunun (`url-shortener`) `AUTOFORGE-FEEDBACK.md`'ye düşürdüğü tek meta-gözlem (commit-queue `nextPending()` atomik claim eksikliği) retro'nun ana teması yapıldı; buradan 2 somut süreç iyileştirmesi (atomik claim [P2] + dokümantasyon notu [P3]) türetildi.
- Değerlendirilen alternatifler: (1) yalnız "iyi gitti" bölümüyle yetinip somut öneri üretmemek — kalite kapısı ("≥1 somut süreç iyileştirmesi") tarafından zaten reddedilirdi; (2) commit-queue düzeltmesini bu faz İÇİNDE fabrika koduna doğrudan uygulamak.
- Gerekçe: Faz 16 kapsamı "FABRİKA değerlendirilir, ürün değil" — üretim (kod değişikliği) değil ÖNERİ üretimi beklenir; fabrika kodunu (`scripts/lib/commit-queue.cjs`) bu ürün pipeline'ının bir fazından değiştirmek kapsam dışıdır (meta-döngü değişiklikleri `/pipeline-improve` + insan onayı akışına aittir, CLAUDE.md "meta-döngü" bölümü). Bu yüzden yalnız öneri yazıldı, kod DEĞİŞTİRİLMEDİ.
- Riskler: Öneri uygulanmazsa aynı yarış koşulu (zararsız ama kafa karıştırıcı çift-commit) başka projelerin de Faz 9 resume'lerinde tekrarlanabilir — düşük olasılık (dashboard+CLI eşzamanlı drain'i nadir), ama sıfır değil.
- Geri alınabilirlik: Yüksek (bu yalnız bir dokümantasyon/öneri kararı; fabrika kodu bu fazda değişmedi).
- İnsan onayı: Otomatik (AUTOPILOT — retro üretimi, kritik risk kabulü değil)
- Varsayım mı?: Hayır — doğrudan `AUTOFORGE-FEEDBACK.md`'deki mevcut kayıttan türetildi.

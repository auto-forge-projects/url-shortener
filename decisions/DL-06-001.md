# DL-06-001: Yüzey sözleşmesi = endpoint sözleşmesi, hata formatı standardı

- Tarih: 2026-07-27
- Faz: 6 — UI/UX
- Mod: AUTOPILOT
- Karar: Ürün tipi service/API olduğu için UI/UX çıktısı ekran değil endpoint tablosu + örnek istek/yanıt dökümü olarak üretildi; tüm hatalar `{"error","message"}` şemasında standardize edildi.
- Değerlendirilen alternatifler: (1) Yalnız Mermaid akış diyagramı (istek/yanıt örneği yok) — reddedildi, geliştirici için yetersiz somutluk.
- Gerekçe: FR-1/2/3 ile birebir eşlenen 3 uçtan-uca örnek oturum, Faz 9 developer'ın test yazımı için doğrudan referans olur.
- Riskler: `short_url` taban URL'i (BASE_URL) henüz mimaride tanımlı değildi — varsayım olarak eklendi.
- Geri alınabilirlik: Yüksek (dokümantasyon, kod etkisi yok)
- İnsan onayı: Otomatik
- Varsayım mı?: Evet — AUTOPILOT varsayımı: `BASE_URL` env yoksa yanıt yalnız `code` döner.
